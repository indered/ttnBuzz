const complaints = (state = {}, action) => {
  switch (action.type) {
    case "ADD_TO_CART": {
    }

    case "PLUS_ITEM": {
    }

    case "REMOVE_FROM_CART": {
    }

    default: {
      return state;
    }
  }
};

export default complaints;
